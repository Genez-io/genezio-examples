name: todo-list-ts
region: us-east-1
language: ts
cloudProvider: genezio
scripts:
  preBackendDeploy: npm install
  preFrontendDeploy: npm run install-prod-sdk && npm install && npm run build
  preStartLocal: (cd server && npm install) && (cd client && npm install)
frontend:
  path: ./client/dist/todo-list
packageManager: npm
workspace:
  backend: server
  frontend: client
